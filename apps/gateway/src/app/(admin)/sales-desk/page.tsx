"use client";

import { SalesDeskWorkspace } from "./SalesDeskWorkspace";

/** Sales Desk — first Workspace Framework consumer (SREQ → SO + tasks). */
export default function SalesDeskPage() {
  return <SalesDeskWorkspace />;
}
