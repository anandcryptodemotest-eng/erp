# Form Designer (Multi-Host)

- Forms are metadata (`layout`, `fields`, `themeId`, `audiences`).
- Set `audiences` so Hosts can discover the form (`ADMIN`, `CUSTOMER`, …).
- Empty audiences ⇒ Admin-only (legacy).
- Publish before Hosts can load; drafts never run at runtime.
