# Create an extension

Use `registerWidget`, `registerActivity`, or `registerTheme` on the Extension Registry. Do not modify `@erp/workflow` engine or `@erp/ui-runtime` core for new capabilities.

Dynamic package loading / marketplace is deferred.
