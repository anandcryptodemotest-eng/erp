# Create a workflow

1. Configuration / Workflows → Clone draft.
2. Connect activities; assign roles.
3. Pick a published FORM AssetRef per HUMAN activity.
4. Validate → Publish (validates referenced screens; writes dependency index).
